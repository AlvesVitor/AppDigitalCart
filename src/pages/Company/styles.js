import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: "#fff",
    alignItems: "center",
    padding: 10,
  },
  areaLoading: {
    flex: 1,
    alignItems: "center",
    justifyContent: "center",
  },
});
